@model IEnumerable<KursavayaDogClub.Models.DISTRICT>

@{
    ViewBag.Title = "Index";
}

<h2>Список районов</h2>

<ul class="pager">
    <li class="previous">@Html.ActionLink("Добавить запись", "Create")</li>
</ul>
<table class="table">
    <tr>
        <th>
            @Html.Label("Район")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DISTRICT_NAME)
        </td>
        <td>
            @Html.ActionLink("Редактировать", "Edit", new { id = item.DISTRICT_ID }) |
            @Html.ActionLink("Подробнее", "Details", new { id = item.DISTRICT_ID }) |
            @Html.ActionLink("Удалить", "Delete", new { id = item.DISTRICT_ID })
        </td>
    </tr>
}

</table>
