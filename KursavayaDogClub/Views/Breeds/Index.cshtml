@model IEnumerable<KursavayaDogClub.Models.BREED>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BREED_NAME)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BREED_NAME)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BREED_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BREED_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BREED_ID })
        </td>
    </tr>
}

</table>
